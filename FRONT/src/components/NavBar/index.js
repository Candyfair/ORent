// === Imports
import SearchBar from "../SearchBar";
import Login from "../Login";

const NavBar = () => {

    return (
        <div>
            <SearchBar />
            <Login />
        </div>
    );
};

export default NavBar;